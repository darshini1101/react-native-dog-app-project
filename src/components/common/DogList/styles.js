import { StyleSheet } from 'react-native';

//Define styles
export default StyleSheet.create({
    articleContainer: {
        margin: 5,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
    },
    
});
